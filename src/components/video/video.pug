include ./video.data.pug

section.video
    h2.video__title= videoOptions.title
    p.video__text= videoOptions.description
    .video__wrapper
        iframe(src=`${videoOptions.src}` title=`${videoOptions.title}` frameborder="0" allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture" allowfullscreen)